# نشر الموقع + لوحة التحكم

الموقع العام (`raheeqkanjo.com`) صفحات ثابتة تخدمها GitHub Pages من مجلد `docs/`.
لوحة التحكم تعمل على Railway (خادم Next.js) وتحمل قاعدة البيانات، والرابط
`raheeqkanjo.com/admin` يحوّل إليها.

هذا يعني أن التعديل في اللوحة يُحفَظ فورًا في قاعدة البيانات، ولا يظهر على الموقع
العام إلا بعد الضغط على **«نشر على الموقع»** في أعلى اللوحة. زر النشر يكتب كل شيء
في التزام (commit) واحد على المستودع، وGitHub Pages ينشره خلال دقيقة تقريبًا.

## الإعداد على Railway (مرة واحدة)

### ١) المشروع
- في https://railway.app أنشئي مشروعًا واختاري *Deploy from GitHub repo*.
- المستودع: `sawafilms20-debug/sawafilms20-debug.github.io`.
- يكتشف Railway ملف `railway.json` ويشغّل `npm run start`.

### ٢) قاعدة البيانات
- داخل المشروع نفسه: *New → Database → PostgreSQL*.
- لا حاجة لأي إعداد آخر: Railway يضبط `DATABASE_URL` تلقائيًا، والتهجيرات
  (migrations) تُطبَّق تلقائيًا في كل نشر — أمر التشغيل نفسه هو
  `node scripts/migrate.mjs && next start`، فلا يوجد أمر يدوي يمكن نسيانه.

### ٣) المتغيّرات (Railway → Variables)

| المتغيّر | مطلوب؟ | الوصف |
|---|---|---|
| `ADMIN_PASSWORD` | نعم | كلمة المرور التي تُنشأ بها حسابك في أول تشغيل |
| `GITHUB_TOKEN` | نعم | مفتاح GitHub بصلاحية **Contents: Read & write** على المستودع نفسه — يستخدمه زر النشر |
| `DATABASE_URL` | تلقائي | يضبطه Railway عند إضافة Postgres |
| `ADMIN_EMAIL` | اختياري | بريد حساب المالكة (الافتراضي `raheeqkanjo@gmail.com`) |
| `ADMIN_NAME` | اختياري | الاسم الظاهر في اللوحة |
| `PUBLIC_API_ORIGIN` | اختياري | عنوان الخادم الذي تُخدَم منه الصور المرفوعة (الافتراضي عنوان Railway) |
| `TRUSTED_PROXY_COUNT` | اختياري | عدد الوسطاء أمام التطبيق، الافتراضي `1` (وهو الصحيح على Railway) |
| `EXTRA_ALLOWED_ORIGINS` | اختياري | نطاقات إضافية يُسمح لها بمناداة الواجهة، مفصولة بفواصل |

> `ADMIN_PASSWORD` يُستخدم مرة واحدة لإنشاء حساب المالكة. بعد ذلك تُغيَّر كلمة المرور
> من داخل اللوحة (الإعدادات ← المستخدمون ← أمان حسابك)، وتغييرها هناك هو المصدر
> الحقيقي — لا تغيّري المتغيّر لاحقًا وتتوقعي أن تتغير كلمة المرور.

### ٤) النطاق
`raheeqkanjo.com` يبقى مشيرًا إلى GitHub Pages. صفحة `docs/admin/index.html`
تحوّل تلقائيًا إلى عنوان Railway، فلا حاجة لتغيير DNS.

## أول دخول

1. افتحي `raheeqkanjo.com/admin`.
2. أدخلي `ADMIN_PASSWORD` — يُنشأ حساب المالكة في هذه اللحظة.
3. من **الإعدادات ← المستخدمون** غيّري كلمة المرور، وفعّلي التحقق بخطوتين إن أردتِ.
4. إن كانت هناك مقالات قديمة بصيغة Markdown في `content/blog/`، سيعرض عليكِ زر
   «نشر على الموقع» استيرادها أولًا ويرفض النشر قبل ذلك — لأن النشر قبل الاستيراد
   يُخفي تلك المقالات عن فهرس المدونة وخريطة الموقع. الاستيراد آمن التكرار.

## ماذا يفعل زر «نشر على الموقع»

في التزام واحد على المستودع:

- يولّد صفحة HTML حقيقية لكل مقال منشور تحت `docs/blog/<slug>/`، ويحذف صفحات
  المقالات التي حُذفت أو صارت مسودات.
- يعيد بناء `docs/blog/index.html` و`docs/sitemap.xml` و`docs/rss.xml` و`docs/robots.txt`.
- يكتب `docs/site-content.json`: التعديلات النصية التي أجريتِها في قسم «الصفحات»،
  وقوائم التوصيات وخطوات العمل والخدمات. يطبّقها `docs/site-content.js` على الصفحة
  عند فتحها؛ وإن غاب الملف أو تعطّل، تظهر الصفحة بنصّها الأصلي كما بُنيت.
- يكتب بيانات SEO مباشرةً داخل `<head>` كل صفحة ثابتة (العنوان، الوصف، صورة
  المشاركة، ومنع الفهرسة) — لأن العنوان الذي لا يراه محرك البحث إلا بعد تشغيل
  JavaScript ليس عنوانًا.

## التطوير محليًا

```
npm install
npm run dev
```

ملف `.env.local` (غير مرفوع للمستودع):

```
ADMIN_PASSWORD=...
GITHUB_TOKEN=...
```

### قاعدة بيانات محلية بلا تثبيت

لتشغيل اللوحة كاملة على الجهاز، شغّلي Postgres يعمل داخل Node نفسه:

```
npm run dev:db
```

ثم أنشئي `.env.development.local`:

```
DATABASE_URL=postgres://postgres:postgres@127.0.0.1:5433/postgres
DB_POOL_MAX=1
PUBLIC_API_ORIGIN=http://localhost:3000
```

`DB_POOL_MAX=1` لأن هذا الخادم المحلي يخدم اتصالًا واحدًا في كل مرة؛ على Railway
اتركي القيمة الافتراضية.

## الفحوصات قبل النشر

```
npm run check
```

يشغّل ثلاثة أشياء بالترتيب:

- `npm run typecheck` — فحص الأنواع.
- `npm run db:check` — يطبّق التهجيرات ويشغّل استعلامات اللوحة الفعلية على نسخة
  Postgres تعمل داخل العملية نفسها. لا توجد قاعدة بيانات على الجهاز ولا في CI،
  فبدون هذا الفحص أول تنفيذ لأي استعلام يكون على الخادم الحقيقي.
- `npm run build` — بناء الإنتاج.
